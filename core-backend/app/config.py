# Basic configuration for Manoeuvre backend
PORT = 8765
DEFAULT_SCREEN_WIDTH = 1920
DEFAULT_SCREEN_HEIGHT = 1080

# Cursor clamp bounds; overlay can override later via handshake
CURSOR_MIN_X = 0
CURSOR_MIN_Y = 0
CURSOR_MAX_X = DEFAULT_SCREEN_WIDTH
CURSOR_MAX_Y = DEFAULT_SCREEN_HEIGHT

